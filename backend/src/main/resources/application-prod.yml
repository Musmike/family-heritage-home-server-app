spring:
  jpa:
    hibernate:
      ddl-auto: validate
app:
  cors:
    allowed-origins:
      - "http://${PROD_SERVER_IP}"
      - "http://localhost"
    allowed-methods:
      - "GET"
      - "POST"
      - "PUT"
      - "DELETE"
      - "OPTIONS"
    allow-credentials: true